========================================================================
DistributedGenerator(Distributed version) 
========================================================================

In order to generate massive dataset more efficiently, we also develop a distributed generator, that is DistributedGenerator, utilizing the power of commodity machines to conduct some tasks in parallel.

DistributedGenerator uses a Server (master) and some working nodes (clients or slaves).


